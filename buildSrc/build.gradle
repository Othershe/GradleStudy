apply plugin: 'groovy'
dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:3.1.3'
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}